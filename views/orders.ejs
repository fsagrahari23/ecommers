<%- include('./partials/header') %>
<% if(user.orders.length > 0){ %>
<div class="container mx-auto  mt-12">
  <h1 class="text-3xl font-bold mb-6 text-center">Your Orders</h1>
  <% user.orders.forEach(order => { %>
    <a href="">
  <div class="bg-white rounded-lg shadow-md p-6 mb-4">
      <h2 class="text-2xl font-semibold">Order #<%= order._id %></h2>
      <p class="text-lg">Total: ₹ <%= order.total %></p>
      <p class="text-lg">Items: <%= order.items.join(', ') %></p>
      <p class="text-lg">Date: <%= order.createdAt.toDateString() %></p>
  </div></a>
  <% }); %>
</div>
<% } else { %>
  <div class="container mx-auto  mt-12">
    <h1 class="text-3xl font-bold mb-6 text-center">You have
      no orders yet!</h1>

  </div>
  <% } %>




<%- include('./partials/header') %>